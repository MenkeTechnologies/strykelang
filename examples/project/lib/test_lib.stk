class TestCache {
    cache: Hash = {a => 1};
    fn get_cache { $self->cache }
}
fn Project::Lib::test_cache_ref() {
    val $o = TestCache()
    val $c = $o->get_cache()
    p "C TYPE:" . ref($c)
    p "C KEYS:" . join(",", keys %$c)
}
