#!/usr/bin/env stryke
# Rosetta Code: Factorial

fn my_factorial($n) {
    $n <= 1 ? 1 : $n * my_factorial($n - 1)
}

fn factorial_iter($n) {
    my $result = 1
    $result *= $_ for 1..$n
    $result
}
