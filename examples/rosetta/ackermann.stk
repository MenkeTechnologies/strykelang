#!/usr/bin/env stryke
# Rosetta Code: Ackermann Function

fn A($m, $n) {
    $m == 0 ? $n + 1
  : $n == 0 ? A($m - 1, 1)
  : A($m - 1, A($m, $n - 1))
}

for my $m (0..3) {
    for my $n (0..4) {
        printf "A(%d,%d) = %d\n", $m, $n, A($m, $n)
    }
}
