#!/usr/bin/env stryke
# Rosetta Code: Ethiopian Multiplication

fn ethiopian($a, $b) {
    my $sum = 0
    while ($a >= 1) {
        $sum += $b if $a % 2
        $a = int($a / 2)
        $b *= 2
    }
    $sum
}

p "17 × 34 = " . ethiopian(17, 34)
