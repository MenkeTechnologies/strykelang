#!/usr/bin/env stryke
# Rosetta Code: Closures/Value Capture

fn make_closures {
    ~> { 1..10 } map {
        my $x = $_
        fn { $x * $x }
    }
}

my @closures = make_closures()
p "Closure results (should be squares 1-100):"
~> { @closures } map { $_->() } join ", " p
