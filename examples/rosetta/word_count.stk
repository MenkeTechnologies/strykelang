#!/usr/bin/env stryke
# Rosetta Code: Word Count

fn word_count($text) {
    my %f
    ~> { $text } lc split /\W+/ grep { length } fore { $f{$_}++ }
    %f
}

my %wc = word_count("The cat sat on the mat. The mat was flat.")
p "Word frequencies:"
~> { %wc } keys sort fore { p "  $_: $wc{$_}" }
