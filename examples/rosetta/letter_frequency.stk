#!/usr/bin/env stryke
# Rosetta Code: Letter Frequency

fn freq($text) {
    my %f
    ~> { $text } lc split // grep { /[a-z]/ } fore { $f{$_}++ }
    %f
}

my %f = freq("The quick brown fox jumps over the lazy dog")
p "Letter frequencies:"
~> { %f } keys sort fore { p "  $_: $f{$_}" }
