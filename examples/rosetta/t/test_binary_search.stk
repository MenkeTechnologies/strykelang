#!/usr/bin/env stryke
# Rosetta Code: Binary Search

fn Rosetta::BinarySearch::find($list, $target) {
    val @arr = @$list
    var $low = 0
    var $high = len(@arr) - 1
    
    while ($low <= $high) {
        val $mid = int(($low + $high) / 2)
        if ($arr[$mid] == $target) {
            return $mid
        } elsif ($arr[$mid] < $target) {
            $low = $mid + 1
        } else {
            $high = $mid - 1
        }
    }
    -1
}

val $data = [1, 3, 5, 7, 9, 11, 13]
assert_eq Rosetta::BinarySearch::find($data, 7), 3, "found 7 at 3"
assert_eq Rosetta::BinarySearch::find($data, 1), 0, "found 1 at 0"
assert_eq Rosetta::BinarySearch::find($data, 13), 6, "found 13 at 6"
assert_eq Rosetta::BinarySearch::find($data, 8), -1, "8 not found"

test_run
