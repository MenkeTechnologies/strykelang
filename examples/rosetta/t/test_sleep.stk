#!/usr/bin/env stryke
# Rosetta Code: Sleep

fn test_sleep {
    my $start = time()
    sleep(1)
    my $end = time()
    my $diff = $end - $start
    # Allow for some minor timing variation
    $diff >= 1
}

assert_ok test_sleep(), "slept for at least 1 second"

test_run
