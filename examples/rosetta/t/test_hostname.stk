#!/usr/bin/env stryke
# Rosetta Code: Hostname

fn Rosetta::Hostname::get() {
    # Using the net_hostname builtin
    net_hostname()
}

my $host = Rosetta::Hostname::get()
assert_ok length($host) > 0, "Hostname is not empty"
assert_eq type($host), "string", "Hostname is a string"

test_run
