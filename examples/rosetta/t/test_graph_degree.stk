#!/usr/bin/env stryke
# Rosetta Code: Node Degree

fn Rosetta::GraphDegree::get($adj, $node) {
    if (!exists $adj->{$node}) { return 0 }
    scalar(@{$adj->{$node}})
}

my $graph = {
    A => ["B", "C"],
    B => ["A", "C", "D"],
    C => ["A", "B"],
    D => ["B"]
}

assert_eq Rosetta::GraphDegree::get($graph, "A"), 2, "degree of A"
assert_eq Rosetta::GraphDegree::get($graph, "B"), 3, "degree of B"
assert_eq Rosetta::GraphDegree::get($graph, "D"), 1, "degree of D"
assert_eq Rosetta::GraphDegree::get($graph, "E"), 0, "degree of missing E"

test_run
