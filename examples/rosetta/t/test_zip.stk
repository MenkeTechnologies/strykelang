#!/usr/bin/env stryke
# Rosetta Code: Zip

fn my_zip($l1, $l2) {
    my @res
    my $len = scalar(@$l1)
    my $len2 = scalar(@$l2)
    if ($len2 < $len) { $len = $len2 }
    
    for my $i (0..$len-1) {
        push @res, [$l1->[$i], $l2->[$i]]
    }
    \@res
}

my @a = (1, 2, 3)
my @b = ("a", "b", "c")
my $zipped = my_zip(\@a, \@b)

assert_eq scalar(@$zipped), 3, "zipped length"
assert_eq $zipped->[0]->[0], 1, "zipped[0][0]"
assert_eq $zipped->[0]->[1], "a", "zipped[0][1]"
assert_eq $zipped->[2]->[1], "c", "zipped[2][1]"

my @c = (4, 5)
my $zipped2 = my_zip(\@a, \@c)
assert_eq scalar(@$zipped2), 2, "zipped uneven length"

test_run
