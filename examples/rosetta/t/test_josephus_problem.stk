#!/usr/bin/env stryke
# Rosetta Code: Josephus problem

fn Rosetta::Josephus::solve($n, $k) {
    my $p = 0
    for my $i (1..$n) {
        $p = ($p + $k) % $i
    }
    $p + 1
}

assert_eq Rosetta::Josephus::solve(41, 3), 31, "Josephus(41, 3) is 31"
assert_eq Rosetta::Josephus::solve(5, 2), 3, "Josephus(5, 2) is 3"

# Chaining
41 |> Rosetta::Josephus::solve(3) |> assert_eq(31, "pipe josephus")

test_run
