#!/usr/bin/env stryke
# Rosetta Code: Caesar Cipher

fn Rosetta::CaesarCipher::encode($str, $shift) {
    my @res
    my @chars = $str |> chars
    for my $c (@chars) {
        if ($c =~ /[A-Z]/) {
            push @res, chr((ord($c) - 65 + $shift) % 26 + 65)
        } elsif ($c =~ /[a-z]/) {
            push @res, chr((ord($c) - 97 + $shift) % 26 + 97)
        } else {
            push @res, $c
        }
    }
    join("", @res)
}

fn Rosetta::CaesarCipher::rot13($str) {
    Rosetta::CaesarCipher::encode($str, 13)
}

assert_eq Rosetta::CaesarCipher::encode("ABC", 1), "BCD", "shift ABC by 1"
assert_eq Rosetta::CaesarCipher::encode("XYZ", 3), "ABC", "shift XYZ wraps"
assert_eq Rosetta::CaesarCipher::encode("Hello, World!", 13), "Uryyb, Jbeyq!", "rot13 hello"

assert_eq Rosetta::CaesarCipher::rot13("Hello"), "Uryyb", "rot13 encode"
assert_eq Rosetta::CaesarCipher::rot13(Rosetta::CaesarCipher::rot13("Test")), "Test", "rot13 is self-inverse"

test_run
