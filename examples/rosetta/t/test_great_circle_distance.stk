#!/usr/bin/env stryke
# Rosetta Code: Great Circle Distance

fn Rosetta::GreatCircle::deg2rad($d) { $d * 3.141592653589793 / 180.0 }

fn Rosetta::GreatCircle::distance($lat1, $lon1, $lat2, $lon2) {
    val $radius = 6371.0 # Earth radius in km
    val $dlat = Rosetta::GreatCircle::deg2rad($lat2 - $lat1)
    val $dlon = Rosetta::GreatCircle::deg2rad($lon2 - $lon1)
    val $a = sin($dlat / 2) ** 2 + cos(Rosetta::GreatCircle::deg2rad($lat1)) * cos(Rosetta::GreatCircle::deg2rad($lat2)) * sin($dlon / 2) ** 2
    val $c = 2 * atan2(sqrt($a), sqrt(1 - $a))
    $radius * $c
}

# Distance between Nashville (36.12, -86.67) and Los Angeles (33.94, -118.40)
val $dist = Rosetta::GreatCircle::distance(36.12, -86.67, 33.94, -118.40)
assert_ok abs($dist - 2886.4) < 1.0, "Nashville to LA distance (~2886 km)"

test_run
