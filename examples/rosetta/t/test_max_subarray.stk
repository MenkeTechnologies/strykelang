#!/usr/bin/env stryke
# Rosetta Code: Maximum subarray sum

fn max_subarray(@arr) {
    if (scalar(@arr) == 0) {
        0
    } else {
        my $max_so_far = $arr[0]
        my $curr_max = $arr[0]
        for my $i (1..scalar(@arr)-1) {
            my $x = $arr[$i]
            $curr_max = $x > $curr_max + $x ? $x : $curr_max + $x
            $max_so_far = $curr_max > $max_so_far ? $curr_max : $max_so_far
        }
        $max_so_far
    }
}

my @data = (-2, 1, -3, 4, -1, 2, 1, -5, 4)
assert_eq max_subarray(@data), 6, "max subarray of (-2, 1, -3, 4, -1, 2, 1, -5, 4) is 6"

my @data2 = (1, 2, 3)
assert_eq max_subarray(@data2), 6, "all positive"

my @data3 = (-1, -2, -3)
assert_eq max_subarray(@data3), -1, "all negative"

@data |> max_subarray |> assert_eq(6, "pipe max_subarray")

test_run
