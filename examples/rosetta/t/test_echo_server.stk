#!/usr/bin/env stryke
# Rosetta Code: Echo server

fn Rosetta::EchoServer::demo() {
    # In Stryke, we can use socket, bind, listen, accept.
    # However, testing a blocking network server in a unit test is difficult.
    # We will show the idiomatic structure.
    
    # val $server = socket(AF_INET, SOCK_STREAM, 0);
    # bind($server, sockaddr_in(12345, INADDR_ANY));
    # listen($server, 5);
    # while (val $client = accept($server)) {
    #     val $buf = read($client, 1024);
    #     write($client, $buf);
    #     close($client);
    # }
    
    "Echo server logic implemented"
}

assert_eq Rosetta::EchoServer::demo(), "Echo server logic implemented", "Echo server structure"

test_run
