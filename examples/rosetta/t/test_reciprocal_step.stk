#!/usr/bin/env stryke
# Rosetta Code: Reciprocal step function

fn reciprocal_step($n) {
    if ($n == 0) { return 0 }
    1.0 / $n
}

assert_eq reciprocal_step(2), 0.5, "step 2"
assert_eq reciprocal_step(4), 0.25, "step 4"
assert_eq reciprocal_step(0), 0, "step 0"

test_run
