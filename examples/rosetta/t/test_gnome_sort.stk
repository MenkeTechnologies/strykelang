#!/usr/bin/env stryke
# Rosetta Code: Gnome Sort

fn Rosetta::GnomeSort::sort($list) {
    var @arr = @$list
    var $i = 0
    val $n = len(@arr)
    while ($i < $n) {
        if ($i == 0 || $arr[$i] >= $arr[$i-1]) {
            $i++
        } else {
            ($arr[$i], $arr[$i-1]) = ($arr[$i-1], $arr[$i])
            $i--
        }
    }
    \@arr
}

val $data = [64, 34, 25, 12, 22, 11, 90]
val $sorted = Rosetta::GnomeSort::sort($data)
assert_eq join(",", @$sorted), "11,12,22,25,34,64,90", "gnome sort"

val $data2 = [1, 3, 2, 5, 4]
$data2 |> Rosetta::GnomeSort::sort |> map { join(",", @{_}) } |> assert_eq("1,2,3,4,5", "pipe gnome sort")

test_run
