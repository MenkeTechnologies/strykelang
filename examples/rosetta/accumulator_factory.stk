#!/usr/bin/env stryke
# Rosetta Code: Accumulator Factory

fn accumulator($init) {
    my $sum = $init
    fn ($n) { $sum += $n }
}

my $acc = accumulator(1)
p $acc->(5)
p accumulator(3)->(4)
p $acc->(2.3)
