#!/usr/bin/env stryke
# Rosetta Code: FizzBuzz

fn my_fizzbuzz($n) {
    $n % 15 == 0 ? "FizzBuzz"
  : $n % 3 == 0  ? "Fizz"
  : $n % 5 == 0  ? "Buzz"
  : $n
}

fn fizzbuzz_list($max) {
    1..$max |> map { my_fizzbuzz($_) }
}
