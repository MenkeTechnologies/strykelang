#!/usr/bin/env stryke
# Rosetta Code: Towers of Hanoi

fn hanoi($n, $from, $to, $via) {
    return if $n == 0
    hanoi($n-1, $from, $via, $to)
    p "Move disk $n from $from to $to"
    hanoi($n-1, $via, $to, $from)
}

p "Towers of Hanoi (3 disks):"
hanoi(3, 'A', 'C', 'B')
