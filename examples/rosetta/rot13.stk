#!/usr/bin/env stryke
# Rosetta Code: ROT13

fn rot13($s) {
    ~> { $s } split // map {
        /[A-Za-z]/ ? chr((ord(uc $_) - 65 + 13) % 26 + (/[a-z]/ ? 97 : 65)) : $_
    } join ""
}

my $t = "The Quick Brown Fox"
p "Original: $t"
~> { $t } rot13 { p "ROT13:    $_" } rot13 { p "Back:     $_" }
