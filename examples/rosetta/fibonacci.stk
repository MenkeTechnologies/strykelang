#!/usr/bin/env stryke
# Rosetta Code: Fibonacci Sequence

fn my_fib($n) {
    return $n if $n <= 1
    my_fib($n - 1) + my_fib($n - 2)
}

fn fib_iter($n) {
    my ($a, $b) = (0, 1)
    for my $i (1..$n) {
        ($a, $b) = ($b, $a + $b)
    }
    $a
}
