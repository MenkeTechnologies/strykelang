#!/usr/bin/env stryke

package Transformer

fn mat_mul($m1, $m2) {
    my $m1_rows = len @$m1; my $m1_cols = len @{$m1->[0]}
    my $m2_rows = len @$m2; my $m2_cols = len @{$m2->[0]}
    my $res = []
    for my $i (0 : $m1_rows - 1) {
        my $row = []
        for my $j (0 : $m2_cols - 1) {
            my $sum = 0.0
            for my $k (0 : $m1_cols - 1) { $sum += $m1->[$i]->[$k] * $m2->[$k]->[$j] }
            push @$row, $sum
        }
        push @$res, $row
    }
    $res
}

fn mat_transpose($m) {
    my $rows = len @$m; my $cols = len @{$m->[0]}
    my $res = []
    for my $i (0 : $cols - 1) {
        my $row = []
        for my $j (0 : $rows - 1) { push @$row, $m->[$j]->[$i] }
        push @$res, $row
    }
    $res
}

fn mat_scale($m, $scalar) {
    my $res = []
    for my $row (@$m) {
        my $new_row = []
        for my $val (@$row) { push @$new_row, $val * $scalar }
        push @$res, $new_row
    }
    $res
}

fn softmax($m) {
    my $res = []
    for my $row (@$m) {
        my $max_val = -1000000000.0
        for my $val (@$row) { if ($val > $max_val) { $max_val = $val } }
        my $new_row = []; my $sum_exp = 0.0
        for my $val (@$row) { my $e = exp($val - $max_val); push @$new_row, $e; $sum_exp += $e }
        for my $i (0 : (len @$new_row) - 1) { $new_row->[$i] /= $sum_exp }
        push @$res, $new_row
    }
    $res
}

fn self_attention($q, $k, $v) {
    my $d_k = len @{$q->[0]}
    my $k_t = Transformer::mat_transpose($k)
    my $scores = Transformer::mat_mul($q, $k_t)
    my $scaled_scores = Transformer::mat_scale($scores, 1.0 / sqrt($d_k))
    my $attention_weights = Transformer::softmax($scaled_scores)
    my $output = Transformer::mat_mul($attention_weights, $v)
    { output => $output, weights => $attention_weights }
}

package main

my $Q = [[1.0, 0.0], [0.0, 1.0]]
my $K = [[1.0, 0.0], [0.0, 1.0]]
my $V = [[2.0, 0.0], [0.0, 2.0]]

my $res = Transformer::self_attention($Q, $K, $V)

# The attention weights should be heavily skewed towards the diagonal (1 -> 1, 2 -> 2)
# scaled score for [1,0]*[1,0] = 1/sqrt(2) = 0.707
# scaled score for [1,0]*[0,1] = 0
# softmax(0.707, 0) = [0.67, 0.33]

my $w = $res->{weights}
~> abs($w->[0]->[0] - 0.67) <(0.01) aeq(1, "softmax row 0 col 0")
~> abs($w->[0]->[1] - 0.33) <(0.01) aeq(1, "softmax row 0 col 1")
~> abs($w->[1]->[0] - 0.33) <(0.01) aeq(1, "softmax row 1 col 0")
~> abs($w->[1]->[1] - 0.67) <(0.01) aeq(1, "softmax row 1 col 1")

my $out = $res->{output}
~> abs($out->[0]->[0] - 1.34) <(0.01) aeq(1, "output row 0 col 0")
~> abs($out->[1]->[1] - 1.34) <(0.01) aeq(1, "output row 1 col 1")

test_run
