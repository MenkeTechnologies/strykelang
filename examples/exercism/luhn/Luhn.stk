#!/usr/bin/env stryke
fn Exercism::Luhn::is_luhn_valid {
    val $c = _ =~ s/\s+//gr
    return 0 unless len($c) > 1 && $c =~ /^\d+$/
    (~> $c rev chars mapi { _ *= 2 if odd _1 ; _ > 9 ? _ - 9 : _ } sum) % 10 == 0
}
