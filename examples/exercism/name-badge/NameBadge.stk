#!/usr/bin/env stryke

fn Exercism::NameBadge::print($id, $name, $dept) {
    val $prefix = defined($id) ? "[#{$id}] - " : ""
    val $d      = uc($dept // 'owner')
    "#{$prefix}#{$name} - #{$d}"
}
