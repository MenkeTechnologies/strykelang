#!/usr/bin/env stryke

fn Exercism::BasketballWebsite::extract_from_path {
    my $cur = _
    for my $k (split /\./, _1) {
        return undef unless ref($cur) eq 'HASH'
        $cur = $cur->{$k}
    }
    $cur
}
