#!/usr/bin/env stryke

fn Exercism::BasketballWebsite::extract_from_path($data, $path) {
    var $cur = $data
    for val $k (split /\./, $path) {
        return undef unless ref($cur) eq 'HASH'
        $cur = $cur->{$k}
    }
    $cur
}
