#!/usr/bin/env stryke
fn Exercism::GuessingGame::compare($secret, $guess) =
    !defined($guess)        ? "Make a guess" :
    $secret == $guess       ? "Correct" :
    abs($secret - $guess) == 1 ? "So close" :
    $secret <  $guess       ? "Too high" :
                              "Too low"
