#!/usr/bin/env stryke
fn Exercism::Gigasecond::add_gigasecond($iso) {
    my $z = $iso =~ /T/ ? "${iso}Z" : "${iso}T00:00:00Z"
    datetime_format_tz(datetime_parse_rfc3339($z) + 1e9, "UTC", "%Y-%m-%dT%H:%M:%S")
}
