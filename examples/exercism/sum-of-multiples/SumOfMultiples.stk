#!/usr/bin/env stryke
fn Exercism::SumOfMultiples::sum_of_multiples($input) {
    my $limit = $input->{limit}
    my $factors = $input->{factors}
    sum(grep { my $n = $_; grep { $_ != 0 && $n % $_ == 0 } @$factors } 1..$limit-1) // 0
}
