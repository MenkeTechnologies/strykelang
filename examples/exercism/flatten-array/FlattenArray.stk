#!/usr/bin/env stryke

fn Exercism::FlattenArray::flatten {
    my @out
    for my $x (@{_}) {
        ref($x) eq 'ARRAY' ? push(@out, @{Exercism::FlattenArray::flatten($x)})
                           : defined($x) && push(@out, $x)
    }
    \@out
}
