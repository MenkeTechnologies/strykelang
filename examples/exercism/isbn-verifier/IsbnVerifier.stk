#!/usr/bin/env stryke

fn Exercism::IsbnVerifier::is_valid {
    val $s = _ =~ s/-//rg
    return 0 unless $s =~ /^\d{9}[\dX]$/
    val @ch  = split //, $s
    val $sum = ~> 0:9 maps { ($ch[_] eq 'X' ? 10 : $ch[_]) * (10 - _) } sum
    $sum % 11 == 0 ? 1 : 0
}
