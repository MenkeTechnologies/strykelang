#!/usr/bin/env stryke
my @_S = ([1, "wink"], [2, "double blink"], [4, "close your eyes"], [8, "jump"])

fn Exercism::SecretHandshake::handshake($c) {
    return [] unless $c =~ /^\d+$/
    my @actions = map { $_->[1] } grep { $_->[0] & $c } @_S
    16 & $c ? [rev @actions] : \@actions
}
