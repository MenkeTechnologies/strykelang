#!/usr/bin/env stryke

fn Exercism::QueenAttack::can_attack {
    my $dx = abs(@_ - @$_1[0])
    my $dy = abs(@_1 - @$_1[1])

    die "queens can't share a position" if $dx == 0 && $dy == 0
    $dx == 0 || $dy == 0 || $dx == $dy
}