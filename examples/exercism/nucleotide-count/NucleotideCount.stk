#!/usr/bin/env stryke
fn Exercism::NucleotideCount::count_nucleotides($strand) {
    my $h = { A => 0, C => 0, G => 0, T => 0 }
    ~> chars($strand) fore {
        die "Invalid nucleotide in strand" unless exists $h->{_}
        $h->{_}++
    }
    $h
}
