#!/usr/bin/env stryke
# {{filename}} — generated by `s_web g migration {{class_name}}`.
#
# PASS 1 emits the migration as a plain class. The `Migration` runtime
# base class + `create_table` / `add_column` DSL land in PASS 4 along
# with the `Migrator` builtin that runs the up/down blocks.

class {{class_name}} {
    fn up {
{{up_block}}
    }

    fn down {
{{down_block}}
    }
}
