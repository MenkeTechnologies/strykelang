<h1>Edit {{singular_title}}</h1>

<%= web_form_with(url => "/{{plural_snake}}/" . ${{singular_snake}}->{id}, method => "patch") %>
{{form_fields}}
    <button type="submit">Update {{singular_title}}</button>
<%= web_form_close() %>

<p>
    <%= web_link_to("Show", "/{{plural_snake}}/" . ${{singular_snake}}->{id}) %>
    <%= web_link_to("Back", "/{{plural_snake}}") %>
</p>
