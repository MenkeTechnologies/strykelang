# {{app_name}} — generated by `s_web new`.

# DB files (per-env SQLite)
db/*.sqlite3
db/*.sqlite3-journal
db/*.sqlite3-wal
db/*.sqlite3-shm

# Logs
log/
*.log

# Tmp
tmp/
*.swp
*.swo
.DS_Store

# Stryke bytecode cache (per-machine, never commit)
.stryke_cache/

# Encrypted credentials master key — must NOT be committed.
config/master.key

# Build artifacts (if you AOT-compile)
target/
