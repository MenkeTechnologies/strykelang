#!/usr/bin/env stryke
# app/helpers/application_helper.stk — view helpers for {{app_name}}.
#
# Functions defined here are auto-imported into every view. The framework
# already ships `link_to`, `form_with`, `button_to`, `image_tag`,
# `content_for`, `flash`, `csrf_meta_tag`, `stylesheet_link_tag`,
# `javascript_link_tag`, and `time_ago_in_words` as builtins — add your
# app-specific helpers here.

# Example:
# fn page_title($default = "{{app_name}}") {
#     content_for(:title) // $default
# }
