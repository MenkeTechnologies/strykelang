#!/usr/bin/env stryke
# bin/server — boot {{app_name}} via the framework's web runner.
#
# Generated by `s_web new`. Edit freely; nothing else depends on the
# exact body — it just needs to start the application.

require "./config/application";
use stryke::web;

my $port = $ENV{PORT} // 3000;
web_server_start($port);
