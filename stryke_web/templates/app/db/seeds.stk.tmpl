#!/usr/bin/env stryke
# db/seeds.stk — seed data, loaded by `s_web db seed`.
#
# Use the model classes to create records. This file is idempotent if you
# use `find_or_create_by`-style lookups; otherwise re-running will create
# duplicates.

# Example:
# Post::find_or_create_by(title => "Welcome") {
#     body => "Hello from {{app_name}}",
#     published => 1,
# }
